streamlit>=1.32.0
opencv-python-headless>=4.9.0
mediapipe>=0.10.30
pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
plotly>=5.20.0

# ---------------------------------------------------------------------------
# Optional: TensorFlow — required only for model training and live inference.
# Not available on Python 3.14 (no wheels); the app boots and runs demo mode
# without it. The Model Evaluation page shows reference metrics as a fallback.
#
# Install locally for training:
#   pip install tensorflow>=2.15.0
#
# Apple Silicon (M1/M2/M3):
#   pip install tensorflow-macos tensorflow-metal
# ---------------------------------------------------------------------------
# tensorflow>=2.15.0
